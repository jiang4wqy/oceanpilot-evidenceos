# 登录与回答精简（2026-09-14）

本轮改动：登录页提供商户 A / 运营专员 A 的快捷入口；删除侧栏宣传语及重复身份/职责说明；材料清单只展示中文名；AI 历史回答将完整依据折叠并合并重复检查提示，原始记录不变；新回答提示词要求先说负责人和下一步。

快捷入口仅在 B 启动脚本显式设置 OCEANPILOT_DEMO_ACCOUNTS 后启用，且只接受本机地址、本机请求及同源提交。使用既有私有账号的正常密码登录，替换旧会话；不在网页中分发密码。外部 URL 不提供快捷入口。普通部署不设置该变量即关闭。风险审核、主管等账号仍使用普通登录。

沿用 RUNBOOK.md 的 pin/start 流程和 recovery-final 独立数据目录，端口 8015；无需重建或重置数据。本轮不更改材料、审批、业务结果或 Feishu 配置。

浏览器验收：两个按钮分别进入商户 A 和运营专员 A；页面重复说明已消失；历史 AI 清单保留中文材料名，完整参考仍可展开。既有人工消息原文保留。真实模型生成的新回答长度未在本轮联网验收，不承诺所有模型输出严格达到字数目标。

自动检查范围：tests/web、tests/api、test_dispute_agent、test_dispute_agent_knowledge、B 演示生命周期；另有历史回复展示回归。未重跑整个仓库测试，先前 2269 通过的结果属于上一版。

本轮结果：相关测试 486 通过、1 跳过（未设置测试用 DEEPSEEK_API_KEY 的联网 agent 测试），另 1 条历史回复展示测试通过，共 487 通过。2 条现有依赖警告；改动文件 Ruff 和 git diff --check 通过。8015 已重新 pin 并启动，数据未重置。本轮变更随 member-b/issue-64-demo-readiness 分支提交，作为 PR #67 合并后的后续优化单独评审；本轮未合并。
