# B 验证与交接记录 — 2026-09-14

本记录区分源代码修改、自动化支持证据和实际浏览器观察。C 尚未独立验收；不是全 Stage 4/5 完成声明，也不是远程部署声明。

## 代码变化

在 f1fc063 隔离工作树上保留并验证 #61–63，不重写已有规则引擎。修改 7 个现有文件，新增 2 个本地演示脚本和 4 个 B 专属测试文件（12 个测试）。不编辑 A 的飞书配置/适配器、C 的既有测试/演示文档。

| 修改 | 目的与验证 |
|---|---|
| collaboration.js 显式选择新增/替换并提交 evidence_id | 原 UI 修订会新增另一有效文件，旧不足材料继续阻断；现按选中文件替换。浏览器观察签收材料 revision 1→2、有效项数保持 1；HTTP 缺字段与错交易两组回归。|
| 上传不确定结果保留原请求 | 同文件/材料选项复用 command_id；改变选项不会丢弃未确认请求。Node 实际上传函数回归；未修改上游 UNKNOWN 状态机。|
| app.js 使用 legacy_conversations | 旧私有历史面板不再重复展示共享 conversations；不同历史含义保留。渲染回归；不把修复前重复显示误写成已证实的数据泄露。|
| presenter 当前任务期限与三期限说明、概况/待办页面 | 采用现有 SLA 责任选择；关闭为历史期限；未知为未确认。自动回归，实际商户提交后主期限由 9/17 切到 9/18，内部审核人接手。|
| RuleData 的 required/critical 接口与表单 | 域层已经支持 critical_evidence，但 HTTP 的 V2.1 DTO 丢了该字段。两份 DTO 补充可选字段，表单能明确指定子集；确认与相同请求重放回归。|
| 操作对应的空白说明字段 | Accept、审核、规则、查询、关闭等分别解释应填写的事实与后果；不预填“已授权”。浏览器和 Node 均确认理由为空、确认框未选。|
| 商户逾期创建 OP_INTERNAL handoff | 持久化、关联现有运营分配，重启去重；不自动 ACCEPT/LOST。新增时钟推进回归。reminder 的字段、scope 和事件键未改；不声称飞书已发送。|
| 样例可用标记、运营系统记录导出入口、文件元信息 | 商户投影视图不含 channel，曾导致样例链接消失；增加明确的布尔能力标记。运营可上传清单中的系统记录导出，商户仍无此选项。显示上传者、类型、字节数、SHA-256 与边界。|
| styles.css 局部响应式修正 | 1200px 以下单列布局仍有旧 grid-column:2，挤出隐式列；阶段文案重叠。补齐列重置，阶段按容器换行。1174、1440、390 CSS px 实看，1440/390 无横向溢出。|

## 实际浏览器范围

浏览器：Codex 内置浏览器；B 操作，独立账号逐次登出/登录。来源接入由 `member_b_rehearsal.py` 使用真实认证 HTTP 完成；业务决定、6 次文件上传（一个初稿、一个替换稿、其余四项）、材料提交均为实际网页操作。

案件 `OPV2-2904b2d6g07450493`；交易 `B-VISA131-2ce0bc40-5c9`；新合成补充 B-SYN-VISA131。初始无材料/审批/提交/财务结果。当前 **v10 / OP_REVIEW / UNKNOWN / NOT_FINAL**，五项有效材料，签收证明两版；没有执行风控批准、主管批准、上游提交或结案。

| 路径 | 结果 | 精确范围 |
|---|---|---|
| 商户提出抗辩 | PASS | 空白依据、选择 CONTEST、显式确认、进入收集证据。|
| 签收缺字段 | EXPECTED BUSINESS BLOCK | 实际上传；提示缺 delivered_at，提交按钮被阻止。|
| 明确替换并补齐完整清单 | PASS | 实际选择文件；普通必需项仍阻断；运营独立登录上传合成地址记录。|
| 提交审核及当前责任 | PASS | v10，独立风控审核员，内部目标 9/18；商户待办完成；没有虚构赢单。|
| 内部范围隔离 | PASS（有限） | OP 内部备注通过 UI 发送；HTTP 确认已保存；商户共享记录无该备注，内部请求 404；另一商户案件请求 404。|
| 刷新/停止/恢复后阅读 | PASS | 同案件、版本、5 个对象 ID/哈希、签收 revision 2、权限、当前期限保持一致。|
| 错交易材料 | NOT RUN（浏览器） | 实际认证 HTTP 样例下载→上传→阻断→显式替换→提交已通过；尚未完成该异常的独立浏览器录制。|
| 接受建议/直接接受/UNKNOWN 查询重试/结案 | NOT RUN（本轮浏览器） | 现有完整自动化集支持；不把自动测试当作这些浏览器路径已走完。|
| 双设备同时操作/飞书回执/实时模型 | NOT RUN | A/C 联调，仍需实际设备、可达 URL、授权接收对象和实时凭据。|

截图位于本工作区兄弟目录 `../member-b-64-handoff/browser-acceptance/`：01 不足材料；02 期限切换（旧布局，已被后续截图取代用于版式）；03 恢复后桌面；04 恢复后窄屏；05 最终材料元信息。01/02 是缺陷与过程证据，不应作为最终版式录屏使用。

API 核验记录：`../member-b-64-handoff/http-{candidate-01,recovery-01,recovery-final}.json`。逐个正常下载五份材料，字节 SHA-256 与保存元信息相同；未直接写入数据库业务状态。最后两次备份均使用 SQLite backup API；完整性检查和恢复哈希通过。账号数据库的登录/已读元数据可能随正常使用变化，不要求运行后整个数据库哈希不变。

## 自动化结果

- f1fc063 基线：2237 passed / 6 skipped / 2 warnings。
- 固定 B 源码后的完整集：2249 passed / 6 skipped / 2 warnings，127.92 秒。独立 `.venv` 的最终结果见同目录 FINAL-RESULTS.md。
- `ruff check src tests scripts/member_b_demo.py scripts/member_b_rehearsal.py`：PASS。
- `ruff format --check src tests scripts/member_b_demo.py scripts/member_b_rehearsal.py`：PASS，279 files。
- `git diff --check`：PASS。
- 已知仓库级 Ruff 的 10 个旧问题仍在两个辅助脚本：build_final_proposal_docx.py / fetch_feishu_ids.py，未擅改其归属。上面通过范围只包含 src/tests 和 B 新脚本。
- 六项跳过：test_agent_api 的 live DeepSeek（1）；Claude live（2）；DeepSeek live（2）；PowerShell 脚本（1）。两项警告：Starlette TestClient/httpx 弃用、AnyIO BlockingPortal 别名弃用。
- 开发中第一次完整集出现 1 个渲染比较失败：测试运行期间改了 CSS，两个 surface 缓存读到不同文件版本。已停止编辑后完整重跑通过；未修改/删除该测试以规避失败。

12 个新增测试覆盖：HTTP 缺字段/错交易替换和 critical 子集重放；逾期内部跟进与重启；私有历史、当前期限、空白授权理由、上传 ID/不确定重试、元信息与样例链接；SQLite 恢复完整性、覆盖拒绝、占用端口拒绝、启动环境隔离。

## 20 核心来源支持

`core-reference-support.csv` 保留 20 行逐项来源、缺口、拟执行路径、合成补充、起始状态、预期阻断与实际执行状态。仅 13 行存在对应种子；不能把 28 个种子或本轮的一个新合成案例算作 20 个核心案例已执行。所有核心引用的独立浏览器复现仍标记 NOT RUN。

本次 B-SYN-VISA131 与 CB-CASE-060 的业务主题相似，但未使用该模板入案，不声称其来源链接已复现。CB-CASE-069 是 N/A 安全主题，不直接当有效争议原因码；CB-CASE-065 的 OCR 描述是未支持功能，结构化替代只能另标合成补充。

`reference-data-manifest.json` 固定实际 62 references / 28 templates / 3 sources 及哈希；其中来源阅读/版本字段是原数据集自带元信息，不代表 B 在本轮重新阅读全文或确认规则仍有现实效力。

## 检查点与未完成条件

- B1：独立代码、依赖、三库、角色、版本清单与备份恢复已提供；8015 暂代 8014，外部访问须与 A 协调。
- B2：上述修复有回归；#61–63 既有安全门保留，完整自动集通过。本轮浏览器只覆盖表中明确范围。
- B3：20 行支持表及每案 15 份可下载样例已提供；真实材料与 PDF/图片适配仍是缺口。
- B4：当前没有收到 C 的可复现业务/UI缺陷单；不能声称已完成 C 独立复测。B 自测缺陷已记录，更多报告按准确版本/案件复现后再修。
- B5：本地可供 C 试录的候选与 runbook 已提供。C 三次独立演练、正式活动要求、飞书联合实测、周五冻结及现场支持仍未完成。

建议整合顺序：f1fc063 已有 #61–63 → B 的核心/UI/示例与回归 → A 独立飞书修改，确认事件与收件人契约 → C 在同代码/数据模型完成三次独立主路径及异常 → 用户明确批准后才提交/推送/发布。任何后续关键修复需对应回归并重录受影响画面。
