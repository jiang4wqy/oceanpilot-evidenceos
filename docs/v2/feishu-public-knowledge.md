# 飞书全群知识助手：当前入口与历史记录

## 最新状态：2026-09-15 18:55 公私隔离与双分支实测

解锁后完成新增真实群测：同一个已绑定用户在群中问“我的案件进度”，只得到
`WEBSITE_HANDOFF`，没有案件数据。问“合成演示流程是什么？”得到
`MODEL_WITH_RETRIEVAL` 与合成流程 `public-v2` 引用，并保留合成／Mock 边界。
这次证据来自 OceanPilot 机器人实际回复和独立发送回执，不是飞书另行展示的
“相关知识”推荐。公开队列现为 9 条 SENT，原有 2 条 UNCERTAIN 未改动。

私聊也已实测接受分支及退回查询，网站状态分别一致；重启后收发正常。
相关修复与更多限制见 `feishu-private-cases.md`。完整 C 验收、固定部署与真实
上游仍未完成。以下 17:56 记录为历史状态，不代表目前接受分支仍未测试。

## 历史状态：2026-09-15 17:56 私聊隔离功能已本地启用

群聊仍只检索六条获准公开的产品指南，不能读取案件或执行业务操作。
私聊现已独立启用：负责人确认绑定了隔离演示商户 A，并通过真实飞书卡片完成
二次确认抗辩；同一网站案件已更新。网站接口上传、送审和退回后，私聊通知取得
真实发送回执。配对、查询和操作均重新核验网站账号及案件权限。

公开的机器人边界和合成流程指南已更新为 `public-v2` 并重启加载，只介绍通用
功能；没有把正在演示的案件、账号、材料或飞书身份导入公开知识库。
私聊当前输出标注为结构化结果，不冒充模型推理；群聊模型开关保持开启。
新版本语料的真实群问答复测仍待客户端解锁，不能复用 16:44 的旧回执充当新版本证据。

本轮全量测试 2496 通过、6 跳过；更多真实验收结果与未完成项见
`feishu-private-cases.md`。接受分支、退回后的实时私聊查询、独立双身份验收、
固定环境与真实上游仍未完成。当前仍是 Mac 临时隧道。

## 历史状态：2026-09-15 16:44 网络恢复与真实问答

公开问答已补齐口语匹配、业务操作引导、可照着问的示例、模型主动拒答和编号引用。
角色说明文档升级为 `public-v2`，明确“运营端”是工作人员工作台，不是额外权限角色。
其他五条语料与私聊未启用的边界不变，真实模型的三类问题已本地验证。

**公网收发已恢复，并完成一条带来源回答的真实群测**：网络复测恢复后，重新启动
HTTP/2 Quick Tunnel，客户端的 DNS、TCP、UDP、API 预检查全部通过。两个飞书回调
已更新且 URL challenge 均返回 200；没有更改系统代理、防火墙、DNS 或证书验证。
16:44 在获准测试群发送“@OceanPilot 测试 材料怎么传”，群内实际显示
`AI 回答（基于公开知识检索）` 与材料上传指南 `public-v1` 引用，发送队列为
`SENT / MODEL_WITH_RETRIEVAL`，并保存了真实飞书回执。新卡片使用新的登录地址。
旧的两条 `UNCERTAIN` 未重发或改写；私聊案件功能仍关闭。

这证明此时的公开问答链路可用，不代表临时隧道长期稳定或完整验收已完成。
Mac 与进程仍须在线；固定 HTTPS 部署、一小时运行及独立 C 验收仍待完成。
详见 `feishu-public-knowledge-iteration-2026-09-15.md`。

## 2026-09-15 本地导入记录（历史状态）

负责人在当前任务明确要求“导入一下”后，首批六条仅含产品使用说明的公开
语料已加载到本地运行环境：`feishu-public-knowledge.approved.json`（`public-v1`）。
范围为产品定位、登录与角色、上传补件、审核反馈、群聊边界和合成演示流程。
其中私聊与真实上游的说明按实际状态修订：私聊尚未启用，演示仍为合成数据／Mock。
没有导入运行案件、账号、附件、真实商户资料或未经核实的银行规则。

公开知识模型开关已开启，复用既有 DeepSeek provider；独立真实模型调用已得到
`MODEL_WITH_RETRIEVAL` 和有效产品来源引用。相关自动测试 104 项通过。
服务已重启加载，公网健康检查及两个回调握手通过。知识库问答的真实群聊完整
验收尚未完成：验收时 Mac 锁屏，需要解锁后发送新问题并核对引用和发送回执。
不要把此前 HELP 卡片的成功回执当作此次知识问答验收。

示例文件仍保留为未批准模板；修改或扩充上线语料仍需核实新的公开范围。
目前依然是本机临时隧道，不是固定环境或私聊案件闭环上线。

历史范围：2026-09-14 最初要求群知识问答与网站业务分离；随后负责人明确批准
增加账号绑定的私聊案件助手。后一方案只扩展私聊，不扩大群权限；材料上传、
审核与上游提交仍在登录后的网站，重要决定保留人工确认。

## 数据与能力边界

群聊适配器只有公开知识对象和独立消息队列，没有案件、账号、附件、业务命令或内部讨论服务的引用。群内使用统一帮助与 `/v2/login` 按钮。网站自行鉴权；不宣称飞书 SSO 或飞书托管。旧业务卡片点击及网页上的旧飞书案件 outbox 调用仍被入口拒绝。新私聊适配器通过独立绑定和权限服务工作，不复用旧版案件授权。

文字问题需要已授权群和 @bot；明确授权的单聊可不带 @。群里的个人身份不授予案件权限。附件不会下载或解析，具体案件、敏感输入和业务指令被引导到网站。敏感文本模式检测是辅助防线，不是完整脱敏证明；主要隔离来自机器人无法访问私有业务数据。

知识清单必须逐条包含 `visibility=PUBLIC_GROUP`、`approved=true`、非空 `approval_reference`、来源、版本与正文。仅接受 `PRODUCT_GUIDE` 或 `PUBLIC_REFERENCE`，不加载运行案件或原先同商户的脱敏知识。批准意味着允许对**整个群**公开，不仅是作者可见或完成脱敏。知识源应由负责人核实其事实、适用范围与公开许可。

`feishu-public-knowledge.example.json` 是待审核模板，不是已经批准的语料。默认没有公开语料，不会自动把仓库的全部规则／参考案例公开。

## 本机配置

签名与握手仍使用 `FEISHU_APP_ID`、`FEISHU_ENCRYPT_KEY`、`FEISHU_VERIFICATION_TOKEN`。新配置与旧商户绑定完全独立：

```text
OCEANPILOT_FEISHU_PUBLIC_KNOWLEDGE_PATH=/absolute/private/path/public-knowledge.json
OCEANPILOT_FEISHU_PUBLIC_GROUPS_JSON={"groups":[{"tenant_key":"synthetic-tenant","chat_id":"synthetic-chat","authorized":true,"allow_replies":true,"authorization_reference":"explicit-owner-approval"}]}
OCEANPILOT_FEISHU_PUBLIC_OUTBOUND=authorized-test
OCEANPILOT_FEISHU_KNOWLEDGE_MODEL=enabled
```

上面仅是结构示例，真实标识和凭据仅放本机配置。外发还需要 `FEISHU_APP_SECRET`；群授权和回复许可必须明确，旧 `OCEANPILOT_V21_FEISHU_TEST_TARGETS_JSON` 不生效。模型开关独立显式开启，只复用网站已配置的模型 provider，不给模型业务服务或工具。缺少模型配置时仍可做检索摘录，但不会标成 AI 生成。

当前检索为词项／中文双字片段匹配，最多返回三个获准来源，不宣称向量检索。模型只接收通过保守检查的问题和检索到的公开来源；只允许引用已检索来源，工具调用、未知引用、敏感输出或格式错误退回来源摘录。这不能保证所有模型结论在语义上都正确，仍需实际问题集核验。

回答状态：`HELP`、`WEBSITE_HANDOFF`、`NO_MATCH`、`RETRIEVAL_ONLY`、`MODEL_WITH_RETRIEVAL`、`RETRIEVAL_FALLBACK`。卡片明确区分模型答案、未调用模型的摘录和模型失败后的摘录。

## 消息队列与恢复

公开知识 initializer 在业务数据库同级创建独立 `feishu-public-knowledge.db`，不访问业务表。签名验证在任何分发之前执行。公开问答回调只入队，模型和飞书调用在后台进行，避免把模型延迟计入回调握手。私聊使用独立加密持久化文件，不把身份绑定写进公共知识表。

队列保存域隔离 event/group 哈希、公开批准依据和语料指纹。待处理问题和 provider 原消息标识使用从 Encrypt Key 域隔离派生的 AES-GCM 密钥加密；被拒问题直接替换为固定网站引导请求，不保存原文。密钥不写数据库。答案和实际 provider 回执留在私有队列供验收，不上传 GitHub。

重复事件返回已有状态；相同 event ID 改变内容返回冲突。固定 event 哈希用作 provider UUID；发送中的工作不会被其他 worker 抢占重发。超时或崩溃中的发送变为 `UNCERTAIN`，不自动重试，也不伪造 `SENT`。过期准备任务变为 `BLOCKED`，保留人工诊断而非无限循环。当前没有公开重试 API。

发出前再次检查群授权和语料指纹。撤销批准／更新语料会阻断旧待发工作；不要通过改数据库绕过。修改群配置需重启 A 管理的服务，已发出的群消息不能因后续撤销自动收回。使用新问题验证新的批准语料，不能把未知发送结果简单当作失败重发。

## 验收仍须真实租户完成

必须分别记录：获准群问题、实际回复及 provider 回执、来源引用、无匹配／模型失败、越权群／旧卡片／附件拒绝、重复与重启、网站两种账号登录和独立 C 验收。测试里的模拟回执不满足这些条件。

演示候选流程：全群通用问题 → 引用公开来源的回答 → 打开网站登录 → 用既有账号进入授权工作台。没有获得可公开语料批准或没有真实回执时，不能宣称公开知识 AI 已上线。
