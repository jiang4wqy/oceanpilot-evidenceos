# 四角色改造验收记录

## 当前权限边界（2026-09-15）

风控专员负责执行、核查与上报；案件规则／期限确认、案件分配和知识候选批准收紧为风控经理权限。IT 管理员仅管理账号、技术配置和合成交易，不继承业务权限，不能读取案件、协作消息或案件更新。飞书与网站共用服务端授权；商户决策和独立复核门禁不变。详见 [迁移与权限说明](architecture-and-migration.md)。

本轮在独立临时数据库上执行全量 `PYTHONPATH=src:. .venv/bin/python -m pytest -q --tb=short`：**2417 passed，6 skipped**（135.31 秒）。跳过项仍是未配置实时模型凭据和缺少 PowerShell。`ruff check src tests`、`ruff format --check src tests` 和 `git diff --check` 均通过。新增／调整的覆盖包括：

- 每个业务命令均拒绝 IT；专员不能确认规则、分配案件或批准知识；经理仍继承执行权限。
- 案件详情、SQL 队列、协作、更新游标均拒绝 IT 读取；旧商户授权和参与者记录不能恢复访问。
- 旧未完成规则确认任务迁至经理待办，清除旧 assignee/assignee_id；保留完成任务、审计和幂等回执，重复启动不重复迁移。
- 已绑定飞书的 IT 账号没有案件可读；角色变更后旧二次确认卡和未发送通知失效。此处使用合成传输，**不代表真实飞书租户收发验收**。
- IT 网页仅展示技术管理，隐藏业务统计与审批；Node 页面运行测试验证，未将 9 月 14 日的真实浏览器截图当成本轮验收。

最终全量运行前后 `src` 与 `tests` 内容指纹一致：`680b0160cf64cd53119f4c474e5ff74954ad5788254209d9d8d847a9505153a7`（按排序文件路径逐文件 SHA-256 后再汇总）。

异常记录：上一轮全量中，旧 V1 用例 `test_agent_turn_analyzes_bound_case_without_creating_a_duplicate` 曾因建案响应缺少 `case_id` 失败一次；单项复测和上述最终全量均通过。未修改该 V1 路径，尚未确认偶发原因，不能宣称已修复此旧问题。

本轮未提交、推送、重启或部署，未改动既有运行数据库、账号密码或 `.env`。启用前先备份并升级重启；当前规则确认仍是单案快照确认，不是企业全局规则制定和发布平台。

下文是 2026-09-14 的历史验收记录，保留当时结果；其中“管理员可处理业务”的旧权限已被上述边界替代，历史截图及测试数量不能代表本轮验收。

## 历史版本（2026-09-14）

当前正式账号只有商户 MERCHANT、风控专员 OPERATOR、风控经理 SUPERVISOR、IT 管理员 ADMIN。原运营与风控初审合并；经理拥有专员权限并管理全局案件；IT 管理员拥有全部后台功能。AGENT 是不可登录的系统身份。

![四角色分工](four-roles.svg)

## 自动化验收

在项目 Python 3.12 环境执行 `PYTHONPATH=src:. .venv/bin/python -m pytest -q --tb=short`：**2280 passed，6 skipped**。跳过项为缺少 DeepSeek/Anthropic 凭据的实时模型测试和缺少 PowerShell 的脚本测试。`ruff check src tests` 与 `git diff --check` 通过。

新增 `tests/api/test_four_roles.py` 验证：

- 四角色权限、管理员后台页面与账号创建/停用审计；旧管理 URL 跳转与旧 API 的 410 响应。
- RISK_OFFICER/DIRECTOR 账号幂等迁移、密码哈希与商户范围保留、仅迁移账号旧会话撤销。
- 活跃任务与参与者迁移，历史审核、完成任务、审计及命令回执不重写，案件 revision 增长。
- 经理和 IT 管理员跨案件读取；晚创建的经理仍可读取已有案件；普通专员和商户保留对象隔离。
- 管理者的全量团队统计不受分页及负责人筛选影响，经理可把案件分配给新加入的获授权专员。
- 管理员可以处理业务，但材料包作者不得通过 APPROVE_PACKAGE 或 FINAL_REVIEW 批准自己的工作；账务登记人与资金核对人分离。
- 管理员的案件详情、协作消息和自动更新入口使用一致的访问权限。

更新既有权限、业务链、HTTP、飞书历史适配器和前端测试。前端增加经理进度统计、姓名转义、负责人筛选和普通专员隐藏团队视图的运行测试。

## 浏览器验收

在独立临时 SQLite、127.0.0.1:8028 测试实例中使用合成账号和两笔案件，已实际操作并观察：

- 风控经理登录后看见两名专员、两笔案件及团队进度。
- 选择一名专员后，案件列表收敛为一笔，团队进度仍保留两人的全量统计。
- IT 管理员登录并进入风控工作台、账号与演示数据页面；四角色下拉框、账号范围和管理入口正确显示。
- 桌面页面截图检查：团队表格和管理员表单无明显遮挡或截断。

浏览器验收没有修改既有业务数据库，也没有发布远端服务。升级运行实例时按 [迁移说明](architecture-and-migration.md) 备份并重启；旧角色账号需要重新登录。当前飞书生产入口继续使用全群知识助手。
