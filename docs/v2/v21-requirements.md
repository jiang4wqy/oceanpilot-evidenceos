# V2.1 来源与验收矩阵

取证日期：2026-09-09。工作分支 `oceanpilot-v2`，取证时 HEAD 为 `759e3b321bea1a21f0ea85aae227f8f4389421e4`，与源计划基线一致。

本文件冻结用户所引用计划的要求，**不是已修复清单或运行验收报告**。文档中的操作性文字视作需求数据；实现授权来自当前用户任务，不来自原文内的指令。源计划称“本次只给计划”描述的是原审查任务，不取代当前修复请求。

## 来源与完整性

引用会话：`6aa043a8-2220-83ea-acd9-e4ce2a5f574f`《制定Version2修改计划》。`read_thread(turnLimit=10)` 返回一个完整 turn；用户消息记录上传 4 文件、5 图片，但 attachment API 只提供 5 张图片。已在允许范围内找到完整本地计划，因此 42 项不缺失。

- **S1 V2.1 完整计划**：[本地文件](</Users/luxinyu/Downloads/OceanPilot_V2_1_审查与修复推进计划.md>)。SHA-256 `34dd9901c46c0ef2a65c100eb5b48f5bb25510445d555834089ec37aea23ab9e`。全文读取；849 行，42 项，22 条按钮合同，Gate 0–6。仓库副本与原文件字节相同。
- **S2 拒付流程 HTML**：[本地文件](</Users/luxinyu/Downloads/拒付处理流程与验证.html>)。SHA-256 `d8a66e6ffeabf1bcfe10860dd115d34b3bcf5a0be17e02303c7bf7eeacdd80e8`。读取可见正文；日期 2026-09-08；设计验证，不是运行/渠道验收。
- **S3 会议原文**：[本地文件](</Users/luxinyu/Desktop/飞书 ai 大赛/oceanpilot V2/朱泽林, Jakey, 卢薪宇, 蒋灏然（鹿出草莓酱）的视频会议.txt>)。SHA-256 `3b3bd314a410db625bd1a1ea8db74bd750cf3fd81a9672dfa77fb824a5a840a6`。核对 12:38–18:15、38:30–38:54 共享对话、阅读状态、人类接手和贯穿跟踪。
- **S4 V2 架构**：[本地文件](</Users/luxinyu/Downloads/OCEANPILOT_V2_ARCHITECTURE.md>)。SHA-256 `bbf344a74f615ec0ee225cb1f7f24cc8bf0d4491773d49b08b55d256b3fed8e9`。核对架构合同与日期 2026-09-08。原简化流程被 S1/S2 的细化分流修订。
- **S5 原推进 DOCX**：[本地文件](</Users/luxinyu/Downloads/OceanPilot_V2_推进说明文档.docx>)。SHA-256 `b35f5a290b61f5a3b9c945ea78eef7b452eb17f282d13eb1de664ed32f94b4fb`。本次仅定位文件和计算 hash；未重新解析正文。

S1 原文冻结副本：[v21-source-plan.md](v21-source-plan.md)。原始 42 个编号保留，不重新编号。42 项合计 **P0 24、P1 18**，按原文分别标注“源码确认”“能力缺口”“待实测”等证据类型，不能统一宣称已复现。

可用性限制：S1 引用的文件名带 `(1)`、`(3)`、`(5)`；找到的本地候选无这些下载后缀。HTML、会议、架构内容与引用吻合，但附件 API 未提供原四文件的字节或 hash，不能证明是完全相同版本。五截图可通过会话附件访问，本次未做截图内容复核。不缺完整计划或上述按钮表；仍缺企业正式渠道规则/SLA、接受授权、资金口径及已授权测试飞书空间的实测证据。

## 本轮不能沿用的旧默认

- 主交互为 **Case Shared Thread**：同案商户、授权 OP、OceanPilot 同处共享线程；明确读者范围。
- `OP_INTERNAL` 独立授权。OP 有权查看处理本案所需的商户材料与共享往来，不能通过切页面取得商户身份。
- 旧 `MERCHANT` / `OPERATIONS` 私聊保留原可见范围；不批量公开，不作为共享上下文。需分享的历史内容走显式授权发布。
- 普通消息、阅读、AI 观察有独立游标，不无意义地改变业务审批版本；材料、规则、授权的实质变化仍使相关批准失效。
- 未响应不等于接受；内部超时不等于失权；非终局不等于新阶段；登记证据代码不等于文件支持事实；审核通过不等于胜诉。

## 42 项可证据化验收矩阵

每一项当前状态为“需求已取证，基线行为及修复结果待测试登记”。原文定位指冻结副本中的行号。测试负责人应补充实际失败→通过证据，不在本表中预先填通过。

| 编号 | 优先级 / 原审查类型 | 修复目标 | 原验收场景 | 原文行 |
|---|---|---|---|---|
| **A01** 切换页面就切换业务身份 | P0 / 源码确认／演示设计边界 | 将演示导演台移到独立入口；商户和 OP 使用服务端确定的独立会话。页面 URL、请求体和客户端角色头不能授予权限。企业 SSO 可后续接入，但基本可信身份和对象级授权是本轮前置条件。 | 以商户会话访问运营／治理页面和命令接口，不得升级身份；修改客户端角色、直接访问 URL 均无效。 | 124 |
| **A02** 商户接口返回完整案件聚合，缺少字段级投影 | P0 / 源码确认 | 定义 MerchantCaseView、OperationsCaseView 和队列摘要；在服务端按字段白名单投影。统一覆盖列表、详情、命令回执、计划、AI 活动、更新流以及未来的附件／导出。内部共用聚合可以保留。 | 自动断言商户的所有响应中不含内部字段；商户 A 不能读取商户 B 的案件、消息、材料或 AI 记录。 | 137 |
| **A03** 固定演示商户与角色名代替真实参与者、负责人 | P0 / 源码确认／产品缺口 | 增加用户—商户—案件参与关系、OP 负责人、风控／主管任务分配与任务到期时间；至少用两个商户、两个运营人员和独立审核人演练。OP 是否能跨团队查看全部案件由企业授权策略决定，不猜测。 | 同商户多用户、不同商户及不同 OP 处理人并行操作，任务能明确归属和交接。 | 150 |
| **C01** 本端私有 AI 对话成了默认入口，共享沟通被放到另一页签 | P0 / 源码确认／与反馈不一致 | 以一个 Case Shared Thread 作为主会话；商户案件咨询、OP 回复与 AI 协助在同一线程。OP 内部备忘、策略讨论和未发布草稿保留 OP_INTERNAL。旧私有记录不追溯性自动公开，需明确迁移／发布策略。 | 商户提问、AI 回答、OP 接手和退回补证，在另一独立会话自动出现且来源清楚；OP_INTERNAL 不向商户泄露。 | 163 |
| **C02** AI 上下文没有读取共享案件往来 | P0 / 源码确认 | 将经权限过滤的共享消息、关联材料和最新人工作业结论纳入上下文，保留 message_id 与引用；内部消息只在内部上下文可用。避免简单拼接全部历史。 | OP 在共享线程明确要求某个补充事实，商户随后追问时 AI 能引用该消息，不重新给泛化清单。 | 176 |
| **C03** 已读与人工升级没有完整状态闭环 | P0 / 源码确认／产品缺口 | 增加按参与者的阅读游标、交付状态及 HumanHandoff 任务，记录负责人、创建原因、接手时间、下次跟进时间与解决结论。阅读动作不应使业务审批版本失效。 | AI 遇到未知规则创建可追踪任务；指定 OP 接手、回复、解决后状态同步；已读不能凭发出消息推定。 | 189 |
| **C04** 商户沟通页将所有非商户消息标为 OceanPayment | P1 / 源码确认 | 统一 actor_type 与显示规则，区分商户、OP 真人、OceanPilot 和系统事件；草稿、建议、确认、已执行用不同标签。 | 同一线程至少显示四类来源；AI 输出不能冒充人工审核结论。 | 202 |
| **W01** NO_RESPONSE 后没有同阶段恢复抗辩或接受的正规出口 | P0 / 源码确认 | 增加核实剩余权利与恢复处理动作，允许经确认恢复决定／举证、授权接受、确认失权或继续跟进；不靠制造结果或新阶段解锁。 | 商户目标过期但外部期限未过，NO_RESPONSE 后可经授权恢复 CONTEST；无需虚构上游事件。 | 215 |
| **W02** 可能把已响应的 CONTEST 覆盖成 NO_RESPONSE | P0 / 源码确认 | 分别定义 decision_response_status 与 evidence_task_status。已有有效决定时禁止按未响应覆盖，保留历史与真实超时任务类型。 | 商户早已选择 CONTEST、后续材料迟交，只触发材料超时／升级，不改变 merchant_decision。 | 228 |
| **W03** 审核建议接受被当成退回补证 | P0 / 源码确认 | 分开 RequestRevision 和 RecommendAccept；后者创建决定确认任务，显示理由和责任信息，不自动替商户接受，也不强制补证。 | 风控建议接受后，商户可查看明确建议并授权接受，或在允许范围继续抗辩；没有虚假的补证要求。 | 241 |
| **W04** 最终审核只有批准，没有否决／退回／暂缓 | P0 / 源码确认／合同缺口 | 新增 FinalReviewDecision：批准、退回材料、退回文书、建议接受、暂缓／升级。每条分支明确责任人、包版本失效与时限处理。 | 主管从待终审状态直接退回文书，OP 修改后产生新包并重新终审；不能绕过双人审核。 | 254 |
| **W05** 所有已知非终局结果都强制进入新阶段 | P0 / 源码确认 | 将 outcome、finality、action_required、next_stage 分开；只有经确认的新阶段事件才创建新 Stage。保留 WAITING_UPSTREAM 和 NEEDS_CONFIRMATION。 | 一次非终局已知事件可以保持当前阶段等待；实际新阶段事件才生成阶段与新期限。 | 267 |
| **W06** OTHER 可以直接被标为终局并通过关闭门槛 | P0 / 源码确认 | UNKNOWN／未映射 OTHER 保持待核实；确有其他终局类型时，先形成明确映射、终局依据和授权确认，再评估关闭条件。 | OTHER+final=true 但无可识别终局依据被拒绝；有正式映射的自定义结果按其合同处理。 | 280 |
| **W07** 撤回／结果事件只能在 WAITING_UPSTREAM 状态登记 | P0 / 源码确认 | 增加标准化上游事件处理器；验证事件来源、案件／阶段关联与覆盖关系，独立于当前 UI 操作页签处理合法异步事件。停止或取消被新事件取代的任务并留痕。 | 材料收集中到达有效撤回，无须先假提交；可核验并走终局／资金或待核实分支。 | 293 |
| **W08** 终局后更正与关闭后重开没有正规路径 | P1 / 源码确认／合同缺口 | 引入授权更正／重开，保留原终局、通知与结案历史；也可创建与原案关联的后续案件。是否重开由事件类型和企业政策决定。 | 已关闭案件收到有效更正后可留痕处理，不能直接改数据库或删除原结案记录。 | 306 |
| **W09** 规则变化后没有重新验证既有抗辩资格 | P0 / 源码确认 | 集中验证 eligibility；规则改变后重新评估决定、任务、清单与包，必要时进入权利待核实。高风险动作执行时重新检查当前资格，而非只在商户第一次选择时检查。 | 先 CONTEST，再把规则改成仅 ACCEPT，系统阻止继续抗辩提交并生成重新确认任务。 | 319 |
| **W10** 后续阶段期限从操作当下 now 重新起算 | P0 / 源码确认 | 保留事件发生时间、OP 实际收到时间、渠道给定截止时间和时区；按该规则选定的起算依据计算，内部操作时间只做审计。未知时限进入待确认。 | 同一事件立即录入与延迟一天录入，正式外部截止一致；不得因建阶段动作产生新权利。 | 332 |
| **W11** 新阶段沿用活跃材料和上一阶段结果，缺少明确适用性判定 | P1 / 源码确认／业务适用性待确认 | 明确 current_stage_outcome 与 last_known_outcome；材料增加阶段适用性与复用批准记录，历史快照只读。重用材料时验证是否支持当前抗辩点。 | 进入新阶段显示当前结果待确认；旧材料经确认适用才贡献准备度，历史仍可查。 | 345 |
| **W12** 接受责任后缺少渠道接受／不抗辩处理回执 | P0 / 源码确认／集成合同缺口 | 增加接受处理任务与明确 Mock 回执：授权、责任金额、已有退款／扣回核查、渠道动作或无需动作的规则依据。仍由上游／确认规则决定终局。 | Accept 后不补证、不自动二次退款；有授权与渠道处理记录，资金与终局仍分别验证。 | 358 |
| **W13** 完成结果／改变决定时批量将所有开放任务标记完成 | P1 / 源码确认 | 任务使用 COMPLETED／CANCELLED／SUPERSEDED／WAIVED 等明确语义，按任务类型和事件影响处理，并记录操作者、原因与关联版本。 | 结果到达取消不再需要的补证任务时显示取消而不是已提交；独立未解决事项不会被无依据抹平。 | 371 |
| **W14** 仅允许接受责任的规则也强制非空举证清单 | P1 / 源码确认 | 根据支持动作和阶段验证材料要求；接受授权信息与抗辩证据清单分别建模。 | 仅可接受的已确认规则允许没有抗辩证据项，但接受授权所需字段仍必填。 | 384 |
| **U01** 操作按钮大多按角色显示而非案件可执行条件 | P0 / 源码确认 | 由服务端提供每案 available_actions：visible／enabled／blocked_reason／owner／required_fields／revision。前端从它渲染，不复制一套业务状态机；后端执行仍重新校验。 | 所有可见启用按钮在对应角色、合法有效输入与当前版本下可执行；不可执行的按钮隐藏、禁用说明或转交负责人。 | 397 |
| **U02** 商户手动决定入口不按当前规则过滤 Accept／Contest | P0 / 源码确认 | 共用服务端动作合同，展示实际可选决定及不允许的理由；不把未知权利当两项都可用。 | 仅 ACCEPT、仅 CONTEST、未知规则三种情况的手动入口与 Agent 提案一致。 | 410 |
| **U03** 历史通知导致关闭检查错误显示已通知 | P0 / 源码确认 | 通知完成基于当前结果／资金版本的有效 delivery 记录；前端直接展示服务端 close_gate，不从历史消息重新推断。 | 通知后再新增资金事件，界面立即显示需重新通知；完成新版本通知才恢复绿色。 | 423 |
| **U04** 待终审阶段的进度条可能回到第一步 | P1 / 源码确认 | 进度展示按真实分支和阶段映射，不能把接受、异常、新阶段强行套一条线性进度。 | 从初审通过到建包待终审进度不倒退；Accept 不显示已经完成抗辩材料审核。 | 436 |
| **U05** 失效的审核记录仍显示普通通过 | P1 / 源码确认 | 显式标明当前有效／历史已失效、证据版本、失效事件，优先显示当前待办审核。 | 已通过后材料变更，原记录明确显示失效且说明原因，不能继续暗示可提交。 | 449 |
| **U06** 必要任务复选框没有实际控制效果 | P1 / 源码确认 | 确认是否需要可选任务；需要则贯通字段和关闭闸门，不需要则删除复选框，写清该任务必需。 | 用户可见的配置都真实影响数据／行为；不存在可更改但被静默忽略的字段。 | 462 |
| **U07** 结果表单默认 OTHER，却无法选择 UNKNOWN | P0 / 源码确认 | 结果不明为明确选项；结果／终局必须依据事件，不默认成功或其他终局。阶段选项依据当前阶段和适用动作生成。 | UNKNOWN 可持续核实且不关闭；当前 REPRESENTMENT 不默认再次选择同阶段。 | 475 |
| **U08** 表单、HTTP DTO 与领域层验证不一致 | P1 / 源码确认 | 统一字段合同、条件必填、长度和本地校验，明确短缺字段而非笼统失败；编写 DTO—form—domain 一致性测试。 | 边界长度、空值和授权条件在前后端一致，错误定位到具体输入项。 | 488 |
| **U09** 不存在或无权访问的案件可能继续显示正在读取 | P1 / 源码确认 | 显式区分 LOADING／NOT_FOUND_OR_FORBIDDEN／RETRYABLE_ERROR／EMPTY；拒绝跨商户请求时不泄露案件存在性。 | 不存在／无权案件显示结束态和返回入口；断网显示重试，不留无限加载文案。 | 501 |
| **U10** 已审核知识候选仍显示人工审核按钮 | P1 / 源码确认 | 候选按状态展示只读结论或待审核动作；重新评审必须是新的、明确的流程。 | 已审核项只有查看记录，不能误点再次审核；重新提交候选保留历史版本。 | 514 |
| **I01** 没有时间驱动的主动 SLA 工作，且同版本运行被复用 | P0 / 源码确认／声明中的功能边界 | 将 time_tick／deadline_due 作为独立调度触发，运行记录绑定观察时间，不只绑定业务 revision；幂等键采用案件＋阶段＋截止类型＋提醒档位。业务版本与消息／观察游标分开。 | 不更改任何案件字段、只推进测试时钟，仍能按期提醒和升级；重复扫描不重复轰炸。 | 527 |
| **I02** SLA 风险主要看商户期限，不看当前真正待办 | P0 / 源码确认 | 把 merchant／internal／external 三类截止绑定阶段与任务责任人；等待上游增加复查时间；已完成任务不再催办，正式失权不能由内部超时推断。 | 商户及时提交后仅 OP 任务到期报警；关闭后没有材料催办；外部超期转权利核实。 | 540 |
| **I03** 材料只有元数据，没有文件内容与内容级 AI 核查 | P0 / 现有明确演示边界／核心能力缺口 | 本轮至少支持少量可控类型的合成材料真实上传、对象存储、hash／版本、预览；优先文本型 PDF／文本／结构化样例。AI 输出事实与材料位置、缺口和冲突。扫描件 OCR、全格式识别可延期。 | 同类型两份材料因内容不同得到不同检查结果；空文件／无关文件／重复文件不能仅凭 code 算完成。 | 553 |
| **I04** 知识候选获批不等于已进入实际 Agent 检索 | P1 / 源码路径缺口／需联调验收 | 统一知识检索接口，显式纳入已脱敏且获批的案例模式，按范围、阶段、规则版本检索；带知识来源和批准版本，不自动改变正式规则。 | 案件 A 结案并获批一个特定模式，案件 B 检索到该知识 ID 并引用；未批准候选不可命中。 | 566 |
| **I05** 编辑 AI 提案后，实际命令没有保留明确的提案来源链接 | P1 / 源码确认／审计缺口 | 增加可信关联元数据并由服务端校验提案所属案件、版本与作用域；保存人类修改摘要，不把客户端传来的任意 ID 当可信证据。 | 点击 AI 提案并修改文案后，审计能追溯原 run、proposal、实际 payload 及确认人。 | 579 |
| **I06** 正常早期案件也显示不能结案等全生命周期阻断 | P1 / 源码确认 | 区分 current_action_blockers、future_close_gates 与风险提醒。主界面只显示阻挡当前下一步的原因，其余在相应阶段展开。 | 新案件能正常发布任务时，不以终局／资金未完成为当前阻断；临近关闭时才展示关闭清单。 | 592 |
| **X01** Feishu 只有入站回调，不是可用的双向协作 | P0 / 现有明确演示边界／集成缺口 | 比赛优先打通一个经授权的测试飞书空间：发卡、正确 thread/case 关联、回调权限、回复／更新、状态回执、重试。Email 先保留明确 mock／人工导入，不能假装已接企业邮箱。 | 真实测试群可见新案卡片，点击／提问后案件更新且群里收到回答；不发送给真实业务商户。 | 605 |
| **X02** 接收上游事件目前基本等于人工创建正式拒付 | P1 / 源码确认／接入边界缺口 | 先建立标准事件信封与本地 synthetic registry 校验；真实来源适配器后接。未知／无法关联事件进待核实，不误建正式案。 | 查询／预警不生成正式拒付；重复事件不重复建案；商户与交易不匹配进入隔离审核。 | 618 |
| **X03** Mock 提交只覆盖立即送达并受理 | P1 / 现有明确演示边界／测试缺口 | 扩展 Mock 故障情景：技术失败、业务拒收、回执超时、已受理但响应丢失；先查询再决定允许重提，不因网络超时重复发送。 | 模拟响应丢失后复用同一业务请求身份，确认已受理时不重提；未受理且仍有权利才重试。 | 631 |
| **F01** 部分胜诉与商户实际资金影响表达不足 | P1 / 源码确认／业务信息缺口 | 在企业确认口径前，明确用 synthetic 金额维度演示 disputed／supported／liable／fee／refund／net，保留币种和原始事件依据；不要让人手填一个净值就宣称完成真实账务对账。 | 部分支持场景能解释争议额与责任额之差；费用、退款及扣回不会混淆或重复计入。 | 644 |
| **E01** 更新时重复读取全量案件与完整快照 | P1 / 源码确认结构／性能需实测 | 列表使用权限过滤后的分页摘要，详情独立查询，消息增量更新，业务快照／日志适度拆分；优化前后用固定 synthetic 数据规模基准比较。 | 验证分页与跨商户过滤正确，测量数据量增长时响应体、P95 延迟和 SQLite 锁等待，报告实测而非预设提升倍数。 | 657 |

## 完整产品级按钮合同

下表逐行取自 S1 第 4 节，共 22 条。它完整列出本轮动作类别，但**没有逐动作所有字段名、类型、长度、枚举、错误码和合法状态的机器规格**；这些仍须在 Gate 0 / U08 结合 API 和领域层固化。不能把“按角色显示”当成可执行条件。

共同记录项：页面、身份、案件阶段/状态、显示条件、启用条件、业务命令、字段合同、成功后双端状态、失败后出口、审计。服务端动作响应至少提供 `visible / enabled / blocked_reason / owner / required_fields / revision`；前端显示与执行服务共用合同，后端执行时重新验证。

| 操作 | 正确出现／启用条件 | 成功后的可观察结果 | 必须避免 |
|---|---|---|---|
| 接收上游事件 | 已认证且授权的 OP；有效来源与交易关联 | 建案或归入既有案，生成处理建议 | 商户建正式案、预警误建正式拒付 |
| 发布商户任务 | 规则／权利／期限已确认，任务内容经核对 | 商户收到同案任务与截止时间 | 无清单先推送、重复通知生成重复任务 |
| 接受责任 | 当前允许 Accept，用户有授权 | 记录决定与授权，进入接受处理，不强制补证 | 自动退款、立即结案 |
| 提出抗辩 | 当前具备资格且有有效授权 | 生成当前阶段材料任务 | 权利未知仍默认可选、改规则后沿用旧资格 |
| 上传／补充材料 | Contest 且任务允许修改 | 可预览文件及版本；AI 内容检查；状态同步 | 仅填代码算真实文件已齐 |
| 修订／撤回材料 | 允许修改且有权限；已送审／冻结时走显式修订 | 旧审核／包失效并明确安排重新审核 | 静默覆盖冻结提交版本 |
| 提交材料给 OP | 必需内容满足当前规则，或有明确批准的例外合同 | 当前版本进入 OP 审核队列 | 重复送审；把材料登记等同上游提交 |
| 初审通过 | 当前待审核，授权风控已查看材料 | 创建建包待办，留审核依据 | 商户自审；缺证照样通过 |
| 退回补证 | 当前处于可退回审核，列明具体缺口 | 商户收到任务、对应材料、期限与说明 | 只改变状态、不生成可做任务 |
| 建议接受 | 有事实理由，经授权风控提出 | 商户／授权人收到新的决定请求 | 自动接受或统一解释为补证 |
| 生成包 | 当前有效初审通过，资格仍有效 | 生成可预览的不可混淆版本 | 所有阶段都给建包按钮 |
| 最终审核 | 当前包待审核，独立授权审核人 | 批准冻结／具体原因退回／暂缓升级 | 只有“批准”没有正确否分支 |
| Mock 提交 | 冻结包、有效批准、当前权利和外部期限均成立 | 明确技术状态和业务受理状态 | 网络失败直接当未受理重发 |
| 登记上游事件／结果 | 事件已核验、案／阶段关联正确 | 同阶段等待／核实／新阶段／终局 | 强制先处于已提交状态才接撤回 |
| 进入新阶段 | 有明确适用的新阶段事件及授权 | 新 Stage、原始事件期限、材料适用性复核 | 所有非终局都新建阶段 |
| 登记资金 | 授权 OP、有效事件引用、币种合同正确 | 可追溯 synthetic 账本；核对／通知失效规则生效 | 误执行真实划扣；重复入账 |
| 财务核对 | 当前业务终局，资料齐备且有权核对 | 匹配／差异／有理由不适用 | 只改绿标志即宣称真实资金到账 |
| 通知商户 | 对应当前需通知的结果／资金版本 | 记录渠道、内容、版本与交付状态 | 历史通知永久满足新版本要求 |
| 关闭 | 服务端完整 close_gate 通过 | OP／系统结案，商户只收到结果 | OTHER 未澄清关闭、用批量完成掩盖未解决任务 |
| 发消息／@AI | 参与者有该线程权限 | 消息出现于同案线程，AI／人工来源明确 | 每句聊天弹出高风险命令确认 |
| 转人工 | AI 无法确认／用户请求且任务未重复创建 | 明确接手人、响应目标、解决记录 | 只有“建议联系人工”文字 |
| 知识提取／审核 | 结案后脱敏；候选待审；审核独立 | 批准模式才进入检索 | 已审核条目仍展示必然失败的按钮 |

## 建议按 Gate 留存证据

| Gate | 主要条目 | 可交付证据与退出标准 |
|---|---|---|
| 0 基线与合同 | 全部 | build SHA/URL/数据库/模型集成模式；编号与动作合同；失败用例；明确 mock、待企业确认。 |
| 1 身份与投影 | A01–A03 | 独立会话，两个商户、两个 OP、独立风控/主管；列表/详情/回执/AI/更新/文件全返回面字段白名单与对象权限。 |
| 2 共享协作 | C01–C03、U01 | 两个独立浏览器不切角色完成发布→收到→共享提问→Contest→材料→OP 更新；内部/旧私聊不可见；阅读/消息不使批准失效。 |
| 3 分支与闸门 | W01–W07、W09–W10、W12、U02/U03/U07 | 每个异常有负责人、下一步、期限或复查时间；无假提交/假结果/假阶段解锁；其余开放 W 路径也必须验收。 |
| 4 AI 实际依据 | I01–I03、C02，扩展 I04/I05 | 合成文件的缺证、矛盾、未知三组内容检查；来源定位、转人工全生命周期；只推进时钟能提醒且去重；模型失败不撤销业务成功。 |
| 5 集成与后半程 | X01/X03/F01 | 经授权测试飞书卡片真实收到并回流；提交响应丢失先查询不重复发送；金额维度与通知版本可解释。未授权/未联调不能写通过。 |
| 6 按钮与冻结 | 剩余 U/C/I/E 项及全回归 | 可见启用动作合法输入成功；断网/模型错/无权案件有结束态；1440/1024/390、缩放/键盘、多会话并发；逐场景证据优先于测试总数。 |

## 必须保留的技术与业务不变量

同案 ID 和单一事实源；商户/参与者权限；SQLite 原子命令、幂等与身份/载荷绑定重放；CAS；规则来源与冻结版本；证据/审批/提交版本关系；未知规则/权限/SLA 显式待确认；模型仅解释/检查/起草/协调；高风险提案经有权人确认后重新校验并审计；正式上游和资金仍为 Mock/Disabled，不把合成数据当企业效果。

## 待企业确认和实现选择

企业待确认：实际渠道角色与团队访问策略、每个 scheme/channel/reason/stage 的正式规则及费用/SLA、接受/代录授权范围、终局/更正/重开政策、资金金额口径、通知交付约定、授权测试飞书对象。

实现可先明确的演练合同：服务端可信独立用户、合成 registry、少量可解析文件类型、任务/消息独立游标、明确的测试 Mock 故障模式。不能以这些实现选择补成企业生产规则。S2 中旧示例“设计通过”仍仅为设计推演，S1 中既有测试数字不作为本轮重跑证据。
