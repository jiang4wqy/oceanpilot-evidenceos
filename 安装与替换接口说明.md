# OceanPilot 可分发版

## 一键启动

1. 安装并启动 Docker Desktop。
2. Windows 双击 `start.bat`；macOS/Linux 终端运行 `./start.sh`。
3. 等待容器健康后打开 <http://localhost:8000/v2/login>。
4. 运行 `show-accounts.bat` 或 `./show-accounts.sh` 查看首次生成的6个演示账号。

首次启动会自动创建 `config.env`、随机数据库密码、数据表、规则目录、
4个 Golden Demo 合成案件和6个随机密码账号。再次启动不会重置数据或密码。

## 一处替换接口

编辑根目录的 `config.env`，然后运行 `restart.bat` 或 `./restart.sh`。

### 数据库

默认 PostgreSQL：

```dotenv
COMPOSE_PROFILES=postgresql
OCEANPILOT_DATABASE_BACKEND=postgresql
OCEANPILOT_DATABASE_URL=postgresql+psycopg://oceanpilot:密码@postgres:5432/oceanpilot
```

切换本地 SQLite：

```dotenv
COMPOSE_PROFILES=
OCEANPILOT_DATABASE_BACKEND=sqlite
OCEANPILOT_DATABASE_URL=
```

两种模式都使用 Docker 命名卷持久化；`stop` 不会删除数据。

### AI 模型 API

任意 OpenAI-compatible 端点：

```dotenv
OCEANPILOT_MODEL_PROVIDER=openai_compatible
OCEANPILOT_CHARGEBACK_LIVE_MODEL=1
OCEANPILOT_MODEL_BASE_URL=https://example.com/v1
OCEANPILOT_MODEL_API_KEY=your-key
OCEANPILOT_MODEL_NAME=your-model
```

DeepSeek 可继续使用 `DEEPSEEK_API_BASE` / `DEEPSEEK_API_KEY` / `DEEPSEEK_MODEL`；
Claude 使用 `ANTHROPIC_API_KEY`。不填密钥或设为 `offline` 时使用确定性离线模式。

### 飞书与公网地址

在同一文件填写 `FEISHU_*`、`OCEANPILOT_PUBLIC_BASE_URL` 及相关授权变量。
四项飞书应用凭据不完整时飞书功能会安全关闭，不影响网站。

## 安全边界

- 包内只有合成数据，不含制作者密钥、历史数据库、日志和账号。
- `OCEANPILOT_V2_UPSTREAM_MODE` 只接受 `mock` 或 `disabled`，不会向真实支付上游提交。
- 若要暴露到公网，请改为 HTTPS，设置 `OCEANPILOT_V21_SECURE_COOKIES=1`，并更换数据库密码。

## 维护命令

- 状态：`status.bat` / `./status.sh`
- 重启并应用配置：`restart.bat` / `./restart.sh`
- 停止：`stop.bat` / `./stop.sh`
- 查看日志：`docker compose --env-file config.env logs -f oceanpilot`
